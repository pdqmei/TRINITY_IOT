#include "wifi_manager.h"
#include "esp_log.h"

static const char *TAG = "WIFI";

void wifi_init(void) {
    // Minimal stub: user may expand with preferred configuration
    ESP_LOGI(TAG, "wifi_init() called - implement WiFi init per project needs");
}
